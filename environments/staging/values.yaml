# Environment-specific values for my-website-2 (staging)
replicaCount: 2

image:
  tag: "latest"

ingress:
  enabled: true
  hosts:
    - host: my-website-2.staging.example.com
      paths:
        - path: /
          pathType: Prefix
